name: Assign Author

on:
  pull_request:
    types: [opened, reopened]
  issues:
    types: [opened, reopened]

permissions:
  issues: write
  pull-requests: write

jobs:
  assign-author:
    uses: lluchkaa/actions/.github/workflows/assign-author.yaml@main
    secrets: inherit
